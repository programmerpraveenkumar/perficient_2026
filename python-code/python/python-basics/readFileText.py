import json

# Open and load the JSON file
with open('data.json', 'r') as file:
    data = json.load(file)

# Print the content
print(data)
print(data['name'])  

# Reading and writing to files.
    #csv 
    #json
    #text
    #html 

#write a pdf file with your name.
    # pdf 

#read the text file and Count occurrences of each word
#Find and Replace a Text in text  File
# Read CSV File and Print Rows
#read the title tag from html
#replace the h1 tag to h2 in html file
